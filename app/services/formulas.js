var path = require('path');
var config = require(path.join(__dirname, '../../config/config'));

exports.criticality = function(type) {
    console.log('## formulas criticality ##');
    //console.log(JSON.stringify(arr) + ' ' + index + ' ' + type);
    var pavementCriticality = {
        Functionality: {
            WEIGHT: 0.55,
            fields: {

            }


        }
    };
    retJson.geometry.coordinates = arr.geometry.coordinates[index];
    return pavementCriticality;

}